include(ProcessorCount)
ProcessorCount(NProc)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DNPROCS=${NProc}")

file(GLOB ai_src "*.cpp")

add_library(ai STATIC ${ai_src})

target_link_libraries(ai PUBLIC omp)
target_include_directories(ai PRIVATE ${localboost_SOURCE_DIR} ${localopenmp_BINARY_DIR}/runtime/src)
